# GeoAttend — Progress Tracker

> Read this first when resuming work. It records what's actually built, what decisions are locked in, and what's next. `PLAN.md` has the full phase breakdown; `docs/` has the underlying specs.

## Status: Phase 6b complete — verified over real HTTP and in the browser — ready to start Phase 7

## Decisions locked in so far
- **Authentication changed from Google OAuth to GeoAttend-managed email/password auth** (2026-08-28, requested after Phase 0, before Phase 1 started). No third-party identity provider. Password hashing: **Argon2id**. Session mechanism unchanged (HttpOnly cookies). No self-service signup in the MVP — accounts are provisioned via the Phase 2 seed script (email + role + initial password), since students/faculty need institution-assigned academic identity (PRN, employee ID, etc.) a signup form can't supply. All six `docs/` files plus `PLAN.md` updated accordingly; see `docs/PRODUCT.md` §4, `docs/SECURITY.md` §5–9/§62/§65, `docs/API.md` §5–6 for the current design.
- **Face recognition finalized**: self-hosted, open-source, via **DeepFace**, model **Facenet512** (512-dim embeddings), detector **retinaface**, similarity threshold **0.30** — see the Phase 3 log below for the evaluation spike and the real dependency issues hit along the way (`tf-keras`, broken `opencv` detector backend, a latent SQLAlchemy model-registration bug).
- Session mechanism: secure HttpOnly cookies (implemented in Phase 1).
- Dev database: Docker Postgres using the `pgvector/pgvector:pg16` image (pgvector 0.8.5 confirmed available via `pg_available_extensions`, not yet `CREATE EXTENSION`'d — that's Phase 3).
- Package managers: `uv` for Python (backend), `npm` for the Next.js frontend.
- Monorepo layout: `docs/` (specs, untouched) + `frontend/` + `backend/` + root-level `PLAN.md`/`PROGRESS.md`/`AGENTS.md`.
- **Backend dev port is 8001, not the 8000 the docs use as an example** (`docs/API.md` §2) — port 8000 on this dev machine is already occupied by an unrelated process. `frontend/.env.example`'s `NEXT_PUBLIC_API_URL` reflects this. Revisit if this ever runs on a different machine where 8000 is free — either port is fine, just keep them consistent.
- Alembic uses the async-native `env.py` pattern (`AsyncEngine` + `connection.run_sync(...)`) directly against `asyncpg`, so no second sync DB driver dependency was needed — the "sequencing risk" noted in `PLAN.md` turned out to have a clean fix rather than requiring a workaround.
- pytest-asyncio is configured with a **session-scoped** event loop (`asyncio_default_fixture_loop_scope = "session"`), not the function-scoped default — the module-level SQLAlchemy async `engine` singleton doesn't tolerate a fresh loop per test (connections get pooled across closed loops → `RuntimeError: Event loop is closed`). Keep this in mind if adding new async fixtures later.

## Environment quirks discovered (not GeoAttend-specific, but relevant if this repo moves machines)
- This machine's global `~/.npmrc` points npm at a corporate (MSCI) Azure Artifacts feed whose auth token is expired, which breaks any `npm install`/`npx` for this unrelated personal project. Fixed with a project-local `.npmrc` (`registry=https://registry.npmjs.org/`) at the repo root — but note npm only reads `.npmrc` from the exact working directory, not ancestor directories, so `create-next-app`'s internal `npm install` (run from inside `frontend/`) needed `npm_config_registry=https://registry.npmjs.org/` set as an env var instead. Prefix any one-off `npm`/`npx` command with that env var if it fails with `E401` inside `frontend/`.
- Port 8000 is occupied by an unrelated pre-existing process on this dev machine — see the port-8001 decision above.

## Phase log

### Phase 0 — Foundational setup — COMPLETE
- [x] Git repository initialized (`main` branch).
- [x] `PLAN.md`, `PROGRESS.md` created at repo root.
- [x] `AGENTS.md` created from `docs/ARCHITECTURE.md` §37.
- [x] `frontend/` — Next.js 16 (App Router, Turbopack) + TypeScript + Tailwind + TanStack Query + Zustand + axios scaffolded. Zustand installed but no store created yet (nothing to hold — first store arrives with Phase 1/5a's workflow state).
- [x] `backend/` — FastAPI layered structure scaffolded (`app/{api/routes,core}`; `models/schemas/services/repositories` intentionally not created yet — no content for them until Phase 1, per `docs/ARCHITECTURE.md` §12 "don't create empty structures prematurely"), `pyproject.toml` via `uv`.
- [x] Docker Compose for Postgres (`pgvector/pgvector:pg16`), verified healthy and vector extension available.
- [x] `.env.example` (both `backend/` and `frontend/`) / `.gitignore` (root + frontend-specific, both excluding `.env*` except `.env.example`).
- [x] Health-check endpoints (`GET /api/v1/health`, `GET /api/v1/health/db`) — verified over real HTTP, not just the test client.
- [x] Settings module (`backend/app/core/config.py`) for configurable thresholds (GPS accuracy, face similarity, verification TTL) — placeholders, values TBD in the phases that need them.
- [x] Test framework: pytest + pytest-asyncio + httpx (backend, with transactional-rollback DB fixture in `tests/conftest.py`) — 2/2 tests passing. Vitest for frontend not yet added (no frontend logic worth unit-testing yet beyond the one query hook exercised manually below; add it in Phase 1 alongside the first real component).
- [x] Global exception handler → `{error:{code,message}}` contract (`backend/app/core/errors.py`); request-id logging middleware (`backend/app/core/logging.py`), confirmed `X-Request-ID` header present on real responses.
- [x] Dev CORS for `http://localhost:3000` — verified via a real preflight + credentialed GET from that origin.
- [x] End-to-end connectivity proven: `frontend` (`src/queries/use-health.ts` + `src/app/page.tsx`) fetches `backend`'s `/health` through axios/TanStack Query; frontend build + lint clean; backend ruff lint + format clean.
- [ ] Arcjet account setup — not started (needs a human to create the account/key; flagging for Phase 1 kickoff rather than blocking Phase 0 on it). Google OAuth credential provisioning is no longer needed — see the authentication decision above.

### Phase 1 — Authentication — COMPLETE
Arcjet explicitly skipped per instruction — `/auth/*` ships without rate limiting for now (tracked as a gap; revisit in Phase 7 or sooner).

Backend:
- [x] `app/models/user.py` — `User` model (`email` unique, `password_hash`, `name`, `profile_image_url` nullable, `role` as a `UserRole` enum → Postgres `user_role`, `is_active`, timestamps). No `google_id`.
- [x] `app/models/session.py` — `UserSession` model backing the session cookie: stores only `token_hash` (sha256 of the raw token), never the raw token. Named `UserSession` (not `Session`) to avoid clashing with `sqlalchemy.orm.Session`.
- [x] `app/core/security.py` — Argon2id password hashing (`argon2-cffi`, library defaults, not yet tuned) + session token generation/hashing helpers.
- [x] `app/core/config.py` — `session_cookie_name`, `session_ttl_seconds` (7 days); no signing secret needed since session tokens are opaque and DB-verified, not JWTs.
- [x] `app/schemas/auth.py`, `app/repositories/{user,session}_repository.py`, `app/services/auth_service.py` (`authenticate()` pays the Argon2 cost even for unknown emails via a dummy hash, so response timing doesn't leak account existence), `app/api/deps.py` (`get_current_user`), `app/api/routes/auth.py` (`POST /auth/login`, `GET /auth/me`, `POST /auth/logout`).
- [x] Alembic migration `19ffe46df3cc` — `users` + `sessions` tables, applied.
- [x] `backend/scripts/seed.py` — idempotent dev seed for 3 users (student/faculty/admin @example.com, password `password123`); Phase 2 will extend it with the full academic graph.
- [x] Tests: `tests/test_auth.py` (integration, 6 tests — login success/wrong-password/unknown-email-parity/unauthenticated/logout/inactive-user, real Postgres via the transactional-rollback fixture) + `tests/test_security.py` (unit, 4 tests — hashing/token primitives, no DB). 12/12 passing. Test emails use a `test-`/`nobody@` prefix distinct from `scripts/seed.py`'s fixture data — there's one shared dev database, not a separate test DB, so this is what actually prevents collisions (deliberate simplification, not an oversight).
- [x] Manually verified over real HTTP with cookies (not just the in-process test client): login sets the cookie, `/me` reads it, wrong password and unknown email return byte-identical `401 INVALID_CREDENTIALS`, logout clears the cookie and invalidates the session server-side.

Frontend:
- [x] `src/lib/types.ts` (`User`, `dashboardPathForRole`), `src/queries/use-current-user.ts` (treats 401 as "not logged in", not an error), `use-login.ts`, `use-logout.ts`.
- [x] `src/app/(auth)/login/page.tsx` — email/password form.
- [x] `src/components/require-role.tsx` — role guard, redirects to `/login` if unauthenticated or to the correct dashboard if the role doesn't match the section.
- [x] Real (non-parenthesized) `student/`, `faculty/`, `admin/` route segments with a guarded `layout.tsx` + placeholder `dashboard/page.tsx` each — chosen over parenthesized route groups because `docs/UI.md` §62 specifies literal `/student/dashboard` etc. URLs, which route groups wouldn't produce.
- [x] Root `page.tsx` now redirects by auth state instead of the Phase 0 health-check placeholder; the now-unused `use-health.ts` hook was deleted.
- [x] Build + lint clean.
- [x] **Manually verified in a real browser by the user**: unauthenticated `/` → `/login` redirect, login → correct dashboard, wrong password shows inline error, role guard redirects away from the wrong section, logout clears the session and going back requires logging in again.

Docs synced to match what was actually built (session mechanism is no longer "TBD"):
- [x] `docs/DATABASE.md` §8 renamed "Password Storage & Sessions", `sessions` table schema added, added to the §46 entity list.
- [x] `docs/API.md` §71 / `docs/SECURITY.md` §74 / `docs/SECURITY.md` §8: removed "exact session mechanism" from open items, pointed at the concrete implementation.

Dev tooling added (not part of the phase plan, but requested alongside it):
- [x] Root-level `scripts/{start-docker,start-backend,start-frontend}.sh` — each resolves its own paths via `dirname "$0"` so they work regardless of invocation context; `start-backend.sh` polls `docker compose exec postgres pg_isready` before starting uvicorn (`--reload` now enabled) so it doesn't race Postgres startup.
- [x] `.vscode/tasks.json` — "GeoAttend: Docker/Backend/Frontend" tasks each with `"panel": "dedicated"` (own persistent terminal, reused on rerun) + a "GeoAttend: Start All" compound task running all three in parallel. Verified by running all three scripts concurrently exactly as the tasks would.

### Phase 2 — Academic data model + seed script — COMPLETE
- [x] Models: `Institution`, `AcademicYear`, `Branch`, `Division`, `Subject`, `Student`, `Faculty`, `ClassEnrollment`, and `ClassOffering` (Python class name deliberately not `Class` — reads oddly next to the `class` keyword everywhere in the codebase; `__tablename__` stays `classes` to match every other doc/endpoint reference). Relationships used for eager loading are declared `lazy="raise"` so any accidental un-eager-loaded access fails loudly instead of silently attempting a lazy load (which breaks or N+1s under async SQLAlchemy).
- [x] Migration `883034a9022d` — all 8 new tables with the FKs/unique constraints/indexes from `docs/DATABASE.md` §9–19 and its §38 indexing recommendations, applied.
- [x] Repositories: one per entity, natural-key lookups for seed idempotency (`get_by_code`, `get_by_institution_and_code`, etc.) plus eager-loaded `get_by_user_id` for `Student`/`Faculty`.
- [x] `app/services/{student,faculty}_service.py` — `get_my_profile()`, 404 `RESOURCE_NOT_FOUND` if the authenticated user has no matching profile (e.g., an admin hitting `/students/me`).
- [x] `GET /students/me`, `GET /faculty/me` — response shapes match `docs/API.md` §10 exactly (nested `user`/`branch`/`division`/`academic_year` briefs).
- [x] `scripts/seed.py` extended: 1 institution → 1 academic year → 1 branch → 1 division → 1 subject → student/faculty profiles for the Phase 1 seed users → 1 class → 1 enrollment. Verified idempotent (re-run produces zero duplicate rows).
- [x] Tests: `tests/test_academic.py` (4 integration tests) — 16/16 total passing. Same test-email-prefix convention as Phase 1 to avoid colliding with `scripts/seed.py`'s fixture data (hit this exact collision again while writing these tests — a real footgun worth remembering, not just a one-off).
- [x] Manually verified over real HTTP against the actual seeded data: `/students/me` and `/faculty/me` return the expected nested shape; a student hitting `/faculty/me` correctly gets 404, not data leakage or a 500.
- [x] `docs/DATABASE.md` §9 updated: dropped `face_embedding_id` from the `students` schema (redundant with `face_profiles.student_id`, §28) — a deliberate simplification, not an oversight.

Deliberately deferred (not in this phase's scope, don't be surprised they're missing): `/students/me/classes`, `/faculty/me/classes` (these are API.md's own "Phase 3 — Academic data" endpoints, distinct from this project's Phase 2; they'll land naturally once something needs them — likely Phase 4), any admin CRUD UI (seed script remains the substitute through at least Phase 7 per `PLAN.md`).

### Phase 3 — Face registration — COMPLETE
- [x] **Model spike** (`backend/scripts/face_model_spike.py`): evaluated Facenet512 vs ArcFace via DeepFace (both 512-dim). Facenet512 showed much cleaner same/different-person separation (0.266 vs 1.005, threshold 0.30) than ArcFace (0.557 vs 0.930, threshold 0.68) on this small, non-rigorous test — **Facenet512 chosen**. This is a smoke-level sanity check, not the real FAR/FRR validation `docs/SECURITY.md` §27 requires before production (needs real classroom conditions/volume).
- [x] **Real dependency gotchas hit and fixed** (all documented in code comments, not just here): (1) TensorFlow 2.21's Keras-3 default breaks `retinaface` — needed the `tf-keras` compat package. (2) `opencv-python` 5.0.0 no longer bundles Haar cascade XML files, breaking DeepFace's default `opencv` detector backend — switched to `detector_backend="retinaface"` explicitly (also more accurate). (3) **A real, previously-latent bug**: `app/models/__init__.py` was empty, so models only referenced by string in a `ForeignKey()` (`Institution`, `AcademicYear`, `Branch`, `Division`, `ClassOffering`, `ClassEnrollment`) were never actually registered with SQLAlchemy in the real running app — reads worked fine, but the first real INSERT/flush needing FK-based table sorting crashed with `NoReferencedTableError`. Fixed by having `app/models/__init__.py` import every model and `app/main.py`/`alembic/env.py` both import `app.models` explicitly. This had been latent since Phase 2 (masked because tests and `scripts/seed.py` happen to import every model directly) and would have hit in Phase 4 regardless — worth remembering if a similar "works in tests, fails for real" gap shows up again.
- [x] Settings for the whole pipeline: model name, detector backend, embedding dimension (512), similarity threshold (0.30, DeepFace's own calibrated default — not yet tuned against real usage), min detection confidence (0.90), upload size/dimension limits.
- [x] `app/core/face_model.py` — `warm_up()` (loads both models once at FastAPI startup via `lifespan`, confirmed via timing: ~8.6s app startup vs. ~30-45s cold-load on first request if not pre-warmed) and `extract_embedding()` (maps DeepFace's `FaceNotDetected` and multi-face/low-confidence cases to `docs/API.md` §53 error codes).
- [x] `FaceProfile` model + migration (enables the `vector` Postgres extension and creates `face_profiles` with a `vector(512)` column) — note: pgvector's Alembic autogenerate omits the `import pgvector.sqlalchemy` line; had to add it by hand or the migration fails at import time.
- [x] `app/services/face_service.py` — validates content-type/size/decodability/dimensions (never trusts the declared MIME type, decodes via OpenCV per `docs/SECURITY.md` §40), then calls `face_model`, then upserts via `face_profile_repository` and flips `student.face_registered`.
- [x] `POST/GET /students/me/face` — response shapes match `docs/API.md` §16-17. Re-registration replaces the profile (upsert, not a new row) — verified both by a dedicated test and by hand.
- [x] `tests/fixtures/face.jpg` — a resized (31KB), MIT-licensed copy of one of DeepFace's own test images, committed so tests exercise the real detection/embedding pipeline end-to-end rather than mocking it (mocking is exactly what would hide detection/embedding regressions).
- [x] 7 new tests (23/23 total passing): register success, replace-on-re-register (scoped by `student_id` — the dev DB is shared, so an unfiltered "count all rows" assertion is wrong here, not just in theory: it broke the moment a second student's profile existed from manual testing), no-face-detected, unsupported content type, non-student caller (404), unauthenticated (401), status defaults to unregistered.
- [x] Manually verified over real HTTP end-to-end against the real seeded student, including confirming re-registration doesn't duplicate the row in Postgres directly.
- [x] Refactored `test_academic.py`'s inline fixture-building into `tests/factories.py`, now shared with `test_face.py`.

Frontend (verified by the user in their own browser — camera access can't be meaningfully automated in this sandbox: no `chromium-cli`, and the Playwright Chromium download stalled/was abandoned):
- [x] `src/components/face/camera-capture.tsx` — reusable (Phase 5b will need the same UX for live verification): explains camera use before requesting permission (`docs/UI.md` §52), live preview with an oval guide overlay, **manual capture only** — no live client-side face-detection guidance ("move closer"/"hold still" per `docs/UI.md` §17 would need its own client-side model evaluation, deliberately out of scope for this phase, not an oversight). Stops all camera tracks on unmount.
- [x] `src/app/student/face-registration/page.tsx` — capture → preview (retake/confirm) → upload → success, surfacing the backend's own error messages directly (they're already written in the plain-language style `docs/UI.md` §3.4 asks for).
- [x] `use-face-status`/`use-register-face` query/mutation hooks; student dashboard now shows a "Register your face" prompt card when unregistered.
- [x] **Verified end-to-end in the user's real browser** (camera access can't be meaningfully automated in this sandbox — no `chromium-cli`, Playwright's Chromium download stalled). Found and fixed a real bug this way that no amount of static analysis would have caught: the video preview showed solid black after granting camera permission. Root cause — a React ref-timing bug in `camera-capture.tsx`: `videoRef.current.srcObject = stream` was set *before* `setState("streaming")`, but the `<video>` element is conditionally rendered and only exists in the DOM once state is already `"streaming"` — so the assignment silently hit a `null` ref and the video element mounted with no source. Fixed by moving the `srcObject` assignment into a `useEffect` keyed on `state === "streaming"`, which runs after React has actually mounted the element; added an explicit `.play()` call too for browsers that need it after an imperative `srcObject` set. Confirmed fixed — full flow (permission → live preview → capture → confirm → success, dashboard prompt disappears afterward) now works.

### Phase 4 — Attendance sessions (faculty side) — COMPLETE
- [x] `app/models/attendance_session.py` — `AttendanceSession` (docs/DATABASE.md §20) + `SessionStatus` enum (`CREATED`/`ACTIVE`/`ENDED`, only `ACTIVE`/`ENDED` actually used by app logic so far — `CREATED` exists for spec fidelity but nothing produces it yet, no "schedule for later" feature in this phase). Added `ended_at` (nullable) beyond the documented schema — needed by the `/end` response shape (§22); `docs/DATABASE.md` §20 updated to note it. Composite indexes `(class_id, status)` and `(starts_at, ends_at, status)` per §38. No background job auto-transitions an expired `ACTIVE` session to `ENDED` — anywhere "is this session currently live" matters (conflict check, student eligibility), the code checks `status == ACTIVE` *and* `now` within `[starts_at, ends_at)`, not `status` alone; this is called out as a known gap below, not silently glossed over.
- [x] Migration `09bee73b8082` — applied. The `session_status` Postgres enum needed no manual fix (unlike Phase 3's pgvector import gotcha) — mirrors Phase 1's `user_role` enum precedent exactly (implicit `CREATE TYPE`/`DROP TYPE` tied to `create_table`/`drop_table`).
- [x] Repositories: `attendance_session_repository` (create/get_by_id/get_active_conflict/list_active_for_student/list_for_faculty, all eager-loading `class_offering.subject` + `faculty.user` in one place via a shared options tuple); `class_offering_repository` gained `get_by_id`/`list_for_faculty`; `class_enrollment_repository` gained `count_by_class_ids` (single grouped-count query, not N+1).
- [x] `app/api/deps.py` — new `get_current_faculty` dependency: the reusable "faculty authorization" piece `PLAN.md` asked for. Returns 403 `FORBIDDEN` for a non-faculty caller — deliberately *not* reusing `faculty_service.get_my_profile`'s 404 (that 404 is for the genuinely-anomalous "FACULTY-role user with no Faculty row" case; a student hitting a faculty-only endpoint is a role mismatch, which `FORBIDDEN` fits better, and matches `PLAN.md`'s own exit criteria wording — "cannot touch another faculty member's session (403)"). Existing `/faculty/me` was deliberately left untouched (still 404-on-no-profile) to avoid changing already-shipped, already-tested Phase 2 behavior.
- [x] `app/services/attendance_session_service.py` — `create_session` runs every check from `docs/API.md` §21 as an explicit sequential `ApiError` (class exists → faculty owns it → time validity → coordinates → radius → conflict), not Pydantic validators — matches `face_service`'s existing style and (unlike a Pydantic `model_validator`) surfaces a specific, directly-displayable message through the same `error.message` field the frontend already reads. `end_session` is idempotent (ending an already-ended session just returns its current state, no error — a double-click shouldn't be a failure mode). `get_session_for_user` branches on role (faculty-owns vs. student-enrolled) for the dual-role `GET /attendance/sessions/{id}`.
- [x] Radius bounds added to `app/core/config.py` as named settings (`session_min/max_radius_meters`, 10–500m) rather than inline numbers, per `AGENTS.md`'s "thresholds live in config" rule — not in the docs, a deliberate addition to stop GPS-accuracy-scale or nonsensically-large radii at creation time.
- [x] Routes: `POST /attendance/sessions`, `GET /attendance/sessions/active` (student-facing — registered *before* the dynamic `{session_id}` route, otherwise Starlette's routing order would swallow "active" as a UUID path param and 422 instead), `GET /attendance/sessions/{session_id}`, `POST /attendance/sessions/{session_id}/end`; plus `GET /faculty/me/classes` and `GET /faculty/me/sessions` (the latter only supports a `status` filter, not the full `class_id`/`from`/`to`/pagination surface `docs/API.md` §23 describes — built just enough for the dashboard's "do I have an active session" need, same deliberate partial-build precedent as `/faculty/me/classes` itself being deferred from Phase 2 to here).
- [x] Response shapes matched exactly to `docs/API.md` §18/§19/§21/§22/§20, including the parts that are easy to get subtly wrong: `class` is a nested object (Python-reserved word, handled via a `serialization_alias`), `faculty` is `{id, name}` on the detail endpoint but name-only `{name}` on the active-sessions list (two distinct small schemas, not one with an optional id — an optional field would serialize as `"id": null` instead of the key being absent, which wouldn't match the docs). All built via `.from_session()` classmethods on the schema (services/routes still just pass ORM objects around, matching the existing convention).
- [x] Tests: `tests/factories.py` gained `create_class_offering`/`create_enrollment`; 22 new tests in `tests/test_attendance.py` (create success/unowned-class/wrong-role/bad-time-range/already-over/radius-out-of-bounds/conflict/unauthenticated, end success/other-faculty/not-found/idempotent, detail for owning-faculty/other-faculty/enrolled-student/unenrolled-student/not-found, active-list includes-eligible/excludes-unenrolled/excludes-future-session, faculty classes/sessions listing). 45/45 total passing.
- [x] Manually verified over real HTTP end-to-end (not just the test client) against the real seeded `faculty@example.com` + `student@example.com` + the seeded "DBMS - CS A" class: create → get detail → duplicate-create correctly 409s → student sees it in their active list with the right (name-only-faculty) shape → end → active list empties → a student attempting to end it correctly gets 403 `FORBIDDEN` before even reaching the ownership check. Caught and fixed one real issue in the process: the backend dev server that was already running had no `--reload` flag (started manually, predating this phase's code), so it was serving stale routes (`{"detail":"Not Found"}`, Starlette's default shape, not ours) until restarted — not a code bug, but worth remembering that a running server without `--reload` will silently mask new routes.

Frontend:
- [x] `src/lib/types.ts` — `SessionStatus`, `SubjectBrief`, `FacultyClass`, `SessionDetail`, `CreateSessionInput`, `SessionCreateResult`.
- [x] `src/queries/use-faculty-classes.ts`, `use-active-faculty-sessions.ts`, `use-create-session.ts`, `use-end-session.ts` — same TanStack Query conventions as Phase 1/3 (query-key exports, invalidate-on-mutate).
- [x] `src/app/faculty/sessions/new/page.tsx` — Create Session form (`docs/UI.md` §27/§28): class dropdown (defaults to the first class, derived at render time rather than via a `useEffect`+`setState` — the newer `eslint-plugin-react-hooks` rule in this project's config flags that pattern, "you might not need an effect"), start/end via `datetime-local` inputs, a text-only location step (`navigator.geolocation.getCurrentPosition`, same explain-then-request-permission shape as Phase 3's camera flow — no map library is wired up, so this is a deliberate simplification of §28's map mockup, not an oversight), radius input, and the backend's own error messages surfaced directly on failure (same pattern as Phase 3).
- [x] `src/app/faculty/dashboard/page.tsx` — added a "Create Session" prompt card (always visible) and an active-session card per currently-active session (subject/class, a LIVE badge, start time, "End Session" button) — deliberately without the present/pending counts `docs/UI.md` §26's mockup shows, since those need Phase 6a's roster endpoint, not built yet.
- [x] Build + lint clean (`npm run build` / `npm run lint`); the one lint finding (`react-hooks/set-state-in-effect` on the class-dropdown default) was fixed by computing the default at render time instead of syncing it via an effect, not suppressed.
- [x] **Verified by the user in their own browser** (geolocation permission, like Phase 3's camera permission, can't be meaningfully exercised in this sandbox): logged in as `faculty@example.com`, created a session via the form, saw it as a LIVE card on the dashboard, ended it successfully.

Deliberately deferred (not this phase's scope, don't be surprised they're missing): the full `GET /faculty/me/sessions` query surface (`class_id`/`from`/`to`/pagination — only `status` is supported); any student-facing UI for `/attendance/sessions/active` (`PLAN.md` explicitly assigns the student-facing "active session card → Continue" entry point to Phase 5a, not here, even though the backend endpoint itself is built and tested now); a map view for location selection.
### Phase 5a — Verification: location slice — BACKEND + FRONTEND BUILT, awaiting the user's in-browser check
- [x] **Resolved the schema gap flagged since Phase 3's planning**: `docs/DATABASE.md`'s only verification-related table (`attendance_verification_attempts`, §27) is an audit log of completed attempts, not the mutable in-flight state machine `docs/API.md` §26-35 describes (`CREATED → LOCATION_VERIFIED → FACE_VERIFIED → COMPLETED/FAILED/EXPIRED`, its own `expires_at`). Added a new `attendance_verifications` table (§27a) for the state machine; the audit-log table itself remains unbuilt (still genuinely optional, not needed to implement the flow).
- [x] `app/models/attendance_verification.py` — `AttendanceVerification` + `VerificationStatus` enum. Face-step columns (result, similarity score) deliberately not added — Phase 5b adds them when it builds that step. A row is created once per `POST .../verification` call and updated in place; resubmitting the location step overwrites the same row rather than creating a new one each time.
- [x] Migration `c277665f8ba0` — applied, same enum pattern as Phases 1/4 (no manual fix needed).
- [x] `app/core/geo.py` — `haversine_distance_meters()`, hand-written (10 lines, no dependency justified for something this small per `AGENTS.md` rule 14).
- [x] Config: renamed the Phase 0 placeholder `location_min_accuracy_meters` (never used, semantically backwards — browser GPS "accuracy" is an uncertainty radius, smaller is better) to `location_max_accuracy_meters` = 50.0 (reject readings less precise than this, per `docs/SECURITY.md` §21's "distance=30m, accuracy=±500m is unreliable" example). `verification_context_ttl_seconds` set to 120 (was a 0/TBD placeholder) — long enough for one location+face attempt, short per `docs/API.md` §35's "must expire quickly."
- [x] `app/repositories/attendance_verification_repository.py` (create/get_by_id) and `app/services/verification_service.py`:
  - `start_verification`: session exists → student enrolled (403 `NOT_ENROLLED`) → session time/state valid → face registered (409 `FACE_NOT_REGISTERED`). Deliberately does *not* check for pre-existing attendance — there's no `attendance` table until Phase 5b, so that check belongs at the eventual `/complete` step, not here.
  - Distinguishes `SESSION_NOT_ACTIVE` (409, session hasn't started yet) from `SESSION_EXPIRED` (409, session's window has passed or it was explicitly ended) — the docs list both codes without fully disambiguating them; this mapping gives genuinely different, more accurate student-facing messages for what are different real situations, at negligible extra cost.
  - `submit_location`: unknown id *and* someone else's real id get an identical 404 `VERIFICATION_INVALID` — don't confirm a verification's existence to the wrong student (`docs/API.md` §40). Then: step-state guard (409 `VERIFICATION_STEP_INVALID` if not `CREATED`/`LOCATION_VERIFIED`) → context expiration (409 `VERIFICATION_EXPIRED`, and the row is flipped to `EXPIRED` in the same request) → haversine distance → independent radius and accuracy gates (both must pass; radius failure takes response priority over accuracy if both fail, matching the order `docs/API.md` §28 lists them in) → result stored either way.
  - A key response-shape subtlety matched from the docs: `POST .../location`'s failure case (`verified: false`) is a **200 OK** with a `verified`/`code`/`message` body, not the app's usual `{"error": {...}}` envelope — `docs/API.md` §28-29 show no error-status wrapper for this, unlike the hard failures above (session/face/enrollment), which do use the standard `ApiError` → non-2xx path. `response_model_exclude_none=True` on the route keeps a success response from showing null `code`/`message`/`allowed_radius_meters` keys the docs' example doesn't have.
- [x] Routes: `POST /attendance/sessions/{id}/verification`, `POST /attendance/verifications/{id}/location` — both added to the existing `attendance` router, both use the student profile inline (no dedicated "get_current_student" dependency — same precedent as Phase 4's student-facing endpoints).
- [x] Tests: `tests/factories.py` gained `create_face_profile` (a placeholder embedding — these tests need "a face is registered," not DeepFace's real pipeline, which `test_face.py` already covers). 16 new tests in `tests/test_verification.py` (start: success/unenrolled/no-face/not-started/ended/not-found/unauthenticated; location: success/outside-radius/poor-accuracy/wrong-student/not-found/expired/wrong-step-state/resubmission-overwrites/unauthenticated). 61/61 total passing.
- [x] Manually verified over real HTTP against the real seeded `faculty@example.com`/`student@example.com`/"DBMS - CS A" data: created a real session at known coordinates, started verification, submitted the exact same coordinates (verified, distance 0.0m), a point ~11.1km away (correctly rejected, `LOCATION_OUTSIDE_RADIUS`), and a good position with poor accuracy (correctly rejected, `LOCATION_ACCURACY_TOO_LOW`) — all three matched the automated tests' expectations exactly.
- [x] `docs/DATABASE.md` updated: new §27a documents the `attendance_verifications` table and why it exists alongside (not instead of) the original `attendance_verification_attempts` concept; added to the §46 entity list.

Frontend:
- [x] `src/lib/types.ts` — `ActiveSession`, `StartVerificationResult`, `LocationVerifyResult`.
- [x] `src/queries/use-active-sessions.ts`, `use-start-verification.ts`, `use-submit-location.ts` — same conventions as every prior phase's query hooks.
- [x] `src/app/student/dashboard/page.tsx` — added an active-sessions list (the minimal student entry point `PLAN.md` asked for): each session shows subject/class/faculty with a "Continue" button that starts a verification context and navigates to its result page once one exists.
- [x] `src/app/student/verify/[verificationId]/page.tsx` — the location step: explain-then-request-permission (same shape as every other browser-permission flow in this app), then submits to the backend and renders one of three outcomes distinctly: verified (success, explicitly says face verification is a later phase), step-level rejection (retryable in place — "Try Again" resets to request location again, since the same verification context is still usable), or a context-level hard error like an expired context (not retryable in place — sends the student back to the dashboard to start a fresh context, since resubmitting location wouldn't help). Read the bundled Next.js docs (`node_modules/next/dist/docs`, per this repo's own `AGENTS.md` warning about this Next.js version) before using `useParams()` for the dynamic `[verificationId]` segment — confirmed no Suspense boundary is needed here since `cacheComponents` isn't enabled in `next.config.ts`.
- [x] Build + lint clean (`npm run build` / `npm run lint`).
- [x] **Verified by the user in their own laptop browser**: logged in as a student with an active session, clicked "Continue", allowed location, confirmed the flow worked end-to-end ("everything is working correctly").
- Real-device (phone) GPS testing was initially blocked on the user's corporate-managed laptop (no local admin rights for LAN port-forwarding; the network also filtered known tunnel services like `cloudflared`/`localtunnel` by port/domain). Resolved by cloning the repo to a second, non-corporate machine instead (a root `README.md` was added with setup + phone/LAN-testing instructions for exactly this) — **verified working on a real phone over the LAN from there**, confirming the location check behaves correctly with genuine GPS accuracy.

Deliberately deferred (not this phase's scope): the face step and `/complete` (Phase 5b, including the still-not-built `attendance` table those need); any UI for the `attendance_verification_attempts` audit-log table (unbuilt); re-checking session state during the location step itself (docs only require re-validating the *verification context's own* expiration there — full session-state revalidation is explicitly the `/complete` step's job, per `docs/API.md` §32).
### Phase 5b — Verification: face + complete — COMPLETE
- [x] `app/models/attendance.py` — `Attendance` + `AttendanceStatus` enum (only `PRESENT` defined — `docs/DATABASE.md` §26 explicitly says not to add `ABSENT`/`MANUALLY_ADJUSTED`/`REVOKED` until there's a real need). Snapshots location/face results at the moment of completion (own `latitude`/`longitude`/`location_accuracy`/`distance_meters`/`face_verified`/`face_score` columns) rather than joining back to the short-lived `attendance_verifications` row for them — matches §24's schema exactly. `UNIQUE(session_id, student_id)` is "the single most important constraint in the system" (`docs/ARCHITECTURE.md` §25) — enforced at the DB level, not just the service's own pre-check.
- [x] `attendance_verifications` gained a `face_similarity` column (the Phase 5a docstring had explicitly deferred this to whenever the face step was built).
- [x] Migration `b459e0202afd` — applied.
- [x] `app/core/face_model.py` gained `compare_embeddings()`, using DeepFace's own `find_cosine_distance` (imported from `deepface.modules.verification`, not reimplemented) so it stays consistent with whatever formula `face_similarity_threshold` was calibrated against.
- [x] Reused rather than duplicated: `face_service.py`'s existing `_decode_image` (Phase 3) was made public (`decode_image`, no underscore) so the new verification-face-step code could import and reuse it directly — first attempt at this accidentally proposed re-adding a copy of it into `face_model.py` instead of reusing the existing one; corrected before writing it.
- [x] `app/services/verification_service.py` gained `submit_face()` and `complete_verification()`:
  - `submit_face`: requires `LOCATION_VERIFIED` or `FACE_VERIFIED` (retry) status, decodes/extracts/compares against the registered embedding, stores `face_similarity` regardless of outcome. Image-quality failures (no face detected, multiple faces, low confidence) are hard 422 `ApiError`s reusing Phase 3's existing validators; a detected-but-non-matching face is a soft `200 {verified: false, code: "FACE_NOT_VERIFIED"}` — same two-tier convention as the location step.
  - `complete_verification`: revalidates everything per `docs/API.md` §32 in the doc's own order (context valid + not expired → session still active → student still enrolled → location *and* face verified, both implied by one `status == FACE_VERIFIED` check → attendance doesn't already exist) even though every one of those already passed earlier — a verification context is evidence to re-check, not a promise to trust. The final insert is wrapped so a `sqlalchemy.exc.IntegrityError` from the DB constraint (the actual race-condition backstop, not just the pre-check above it) is caught and converted to the same `ATTENDANCE_ALREADY_MARKED` response.
- [x] Routes: `POST /attendance/verifications/{id}/face` (multipart), `POST /attendance/verifications/{id}/complete` — both added to the existing `attendance` router.
- [x] Tests: `tests/test_attendance_completion.py`, 17 new (start/submit-face: success, mismatch [a deliberately-orthogonal random registered embedding, since there's only one real face fixture image to test with], no-face-detected, before-location, wrong-student, not-found, expired, unauthenticated; complete: success, before-face-verified, wrong-student, expired, ended-session, duplicate-marking, a dedicated DB-constraint test, not-found, unauthenticated). 78/78 total passing. The DB-constraint test calls `attendance_repository.create()` directly a second time for the same key (bypassing the service's own pre-check) to prove the constraint itself is what stops it — genuine multi-connection concurrency (two literally-simultaneous requests) isn't feasible to test with this project's shared-transaction test fixture (`db_session`/`client` both bind to one connection, which can't simulate two independent connections racing the same insert), so this is the closest direct proof available that the actual safety net (not just the application-level check) exists and works; it emits one harmless `SAWarning` during fixture teardown, documented in a test comment rather than silently ignored.
- [x] Manually verified over real HTTP end-to-end against the real seeded `faculty@example.com`/`student@example.com` data: re-registered the seeded student's face with `tests/fixtures/face.jpg` (a known-good image to verify against), then ran the complete session → start → location → face → complete chain, confirming a real `PRESENT` attendance row, and confirmed a second attempt (a fresh verification context for the same session) correctly gets 409 `ATTENDANCE_ALREADY_MARKED`. Note: this overwrote the student's real registered face (from the user's own Phase 3 browser testing) with the test fixture image — re-register your own face again via the UI if you want your real one back for future manual testing.

Frontend:
- [x] `src/lib/types.ts` — `FaceVerifyResult`, `CompleteAttendanceResult`.
- [x] `src/queries/use-submit-face.ts`, `use-complete-verification.ts` — same conventions as every prior phase's query hooks.
- [x] `src/app/student/verify/[verificationId]/page.tsx` — restructured from a location-only page into a `location → face → completing → done` phase machine. The face phase reuses `CameraCapture` (Phase 3) and mirrors `face-registration/page.tsx`'s capture → preview → confirm pattern exactly, rather than building the "automatic capture with face guidance" UI `PLAN.md` describes — same deliberate scope reduction as Phase 3's own registration flow (real-time client-side face-detection guidance needs its own model/library evaluation, not a natural extension of this phase's backend work). The `completing` phase has no user-facing step at all: `/complete` needs no additional input, so it fires automatically via a `useEffect` keyed on the phase transition as soon as the face step succeeds.
- [x] Build + lint clean (`npm run build` / `npm run lint`).
- [x] **Verified by the user on their other (non-corporate) device**, the same one proven out for Phase 5a's phone/LAN testing: full "Continue" → location → face capture → automatic completion → "Attendance marked" flow confirmed working on a real phone.

Deliberately deferred (not this phase's scope): the `attendance_verification_attempts` audit-log table (still unbuilt, still optional); liveness/anti-spoofing (explicitly post-MVP per `docs/ARCHITECTURE.md`, `docs/PRODUCT.md` §12, `docs/SECURITY.md` §28); real multi-connection concurrency testing of the duplicate-attendance race (constrained by the test fixture design, see above — the DB constraint itself is proven, just not via literally-concurrent requests).
### Phase 6a — Faculty live monitoring + rich dashboards — COMPLETE
- [x] Resolved a real scope-boundary question before writing any code: `PLAN.md`'s "rich student dashboard (attendance %, subject breakdown)" sounded like it overlapped with Phase 6b's dedicated `GET /students/me/attendance/summary`. Re-reading `docs/API.md` §11 clarified these are two different endpoints — `/students/me/dashboard` (this phase) returns a simple `{percentage, present, total}` overall figure and a `today_classes` list, while the full subject-by-subject breakdown is `/attendance/summary`'s job, still correctly deferred to Phase 6b. Neither endpoint needs backdated seed data to be *correct* (the math works on whatever real data exists) — just to look rich, which is explicitly Phase 6b's job ("extend the seed script with backdated attendance rows").
- [x] `GET /attendance/sessions/{id}/attendance` (docs/API.md §24, faculty-only, ownership-checked like every other session endpoint). Each enrolled student is classified `PRESENT` (has an `Attendance` row) / `VERIFICATION_ISSUE` (started at least one `AttendanceVerification` attempt but never completed one) / `NOT_MARKED` (neither) — this three-way split isn't a persisted column anywhere, it's computed fresh per request from three simple queries (enrolled students, this session's attendance rows, this session's attempted-student-ids) combined in Python, which is fine at this scale (one division's worth of students).
- [x] `GET /students/me/dashboard` and `GET /faculty/me/dashboard` (docs/API.md §11/§43). Both reuse existing response schemas (`ActiveSessionItem`, `SessionDetailResponse`) for their nested session objects rather than inventing near-duplicates, since the docs give no shape guidance for `today_classes`/`upcoming_classes` beyond an empty example list. "Today" is computed as `[midnight UTC, midnight UTC + 1 day)` — this app has no institution-timezone setting, and `AGENTS.md` already establishes UTC storage/computation as the project convention, so this is consistent rather than a new decision. There's no `Timetable` entity in this schema (explicitly listed as potential-post-MVP in `docs/DATABASE.md` §46), so "classes today" is really "attendance sessions scheduled for today" — a class with no session created for it today simply doesn't appear, which is the correct behavior given how session creation actually works in this app (ad hoc, not pre-scheduled).
- [x] New repository queries: `class_enrollment_repository.list_students_for_class`, `attendance_repository.{list_by_session, count_by_student, count_ended_sessions_for_student}`, `attendance_verification_repository.list_student_ids_with_attempts`, `attendance_session_repository.{list_for_faculty_between, list_for_student_between}`.
- [x] Tests: `tests/test_roster.py` (7: not-marked default, verification-issue for an incomplete attempt, present after a full flow, wrong-faculty, not-found, wrong-role, unauthenticated) and `tests/test_dashboards.py` (11, covering both dashboards' zero-state, populated-state, active/upcoming session detection, and auth/role checks). 96/96 total passing.
- [x] Manually verified over real HTTP against the real seeded data: faculty dashboard correctly went from all-zero to `{classes:1, active_sessions:1}` after creating a session and back to `{active_sessions:0}` after ending it; the live roster correctly flipped one seeded student from `NOT_MARKED` to `PRESENT` after a real location→face→complete flow while the other stayed `NOT_MARKED`; the student dashboard's attendance percentage reflected real accumulated history from every prior phase's own real-HTTP testing (14.3%, 1/7) — a genuine, not fabricated, sanity check that the counting logic is correct across real accumulated data, not just a fresh test fixture.
- [x] Frontend: `src/app/faculty/sessions/[sessionId]/page.tsx` — the live roster view (present/total progress bar, not-marked/verification-issue counts, client-side search-by-name-or-PRN over the already-fetched roster rather than a server-side search param, since roster sizes are small enough that this is simpler and just as responsive). Polls every 5s via `refetchInterval` (docs/API.md §25: polling, not WebSockets/SSE, until real-time requirements justify the complexity). Both dashboard pages rewritten to use the new endpoints; the old Phase 4/5a `useActiveFacultySessions`/`useActiveSessions` hooks (list-shaped, superseded by the new dashboards' singular `active_session`) were deleted rather than left unused, and the mutations that used to invalidate their query keys (`useCreateSession`, `useEndSession`, `useCompleteVerification`) now invalidate the new dashboard query keys instead.
- [x] Build + lint clean (`npm run build` / `npm run lint`).
- [x] **Verified by the user in their own browser.**
### Phase 6b — History — COMPLETE
- [x] `GET /students/me/attendance` (docs/API.md §14): paginated (`page`/`page_size`, max 100), filterable by `subject_id` (the Subject's id, joined through the session's class — docs consistently distinguish "subject" from "class"), `status`, and `from`/`to` (whole UTC day boundaries, matching the "today" convention from Phase 6a). Backed by a new `attendance_repository.list_for_student()` doing a single count query + a single paginated, eager-loaded items query rather than loading everything into Python.
- [x] `GET /students/me/attendance/summary` (docs/API.md §13): `overall` reuses the dashboard's existing `AttendanceOverview` shape; `subjects` is computed via two grouped-by-class queries (`attendance_repository.count_present_by_class_for_student`, `attendance_session_repository.count_ended_by_class_ids`) over the student's enrolled classes (`class_enrollment_repository.list_classes_for_student`, new) rather than one query per class.
- [x] `GET /students/me/classes/{class_id}/attendance` (docs/API.md §15): 403 `FORBIDDEN` if the student isn't enrolled in that class (a real ownership check, unlike the two endpoints above which are inherently self-scoped and have no `class_id`/`student_id` the caller could manipulate to see someone else's data — see the dedicated leakage test below). Reuses the same per-class counting queries as the summary endpoint, plus an unpaginated `attendance_repository.list_for_student_and_class()` for the class's own `records` list.
- [x] Faculty attendance history reuses what already existed rather than adding a new endpoint: `GET /faculty/me/sessions?status=ENDED` (built in Phase 4) plus the roster endpoint (built in Phase 6a, and it never checked session status, so it already worked for ended sessions). The only backend change needed was adding `status` to `RosterSessionBrief` (a small, deliberate addition in the same spirit as `AttendanceSession.ended_at` in Phase 4) so the frontend can tell a live roster from a historical one.
- [x] `scripts/seed.py` extended with two more subjects/classes (Operating Systems, Artificial Intelligence) and ~10 backdated ended sessions each (a fixed, deterministic present/absent pattern — no `random` dependency needed for something this small), enrolling the seeded student in both. DBMS deliberately gets no synthetic backfill — it already has genuine accumulated history from every prior phase's own manual HTTP testing, and mixing synthetic rows into that would make the "is the counting logic actually correct against real data" check from Phase 6a less meaningful. The backfill is idempotent by checking whether the class already has any sessions at all, not a per-session natural key.
- [x] Tests: `tests/test_history.py`, 13 new (history: pagination, subject_id filter, date-range filter, page_size-over-100 rejected, unauthenticated; summary: zero-state for enrolled classes with no sessions yet, correct overall + per-subject percentages, unauthenticated; class detail: summary + records, rejects an unenrolled student, not-found, unauthenticated; a dedicated "doesn't leak another student's attendance" test — two students, only one has real attendance, the other's history is empty and their summary correctly shows 0 *present* despite a nonzero *total* denominator from a class they're enrolled in but didn't attend). 109/109 total passing. Backdated sessions needed for these tests are inserted directly via the ORM (same reasoning as the seed script — `POST /attendance/sessions` can't produce a session whose window is already in the past).
- [x] Manually verified over real HTTP against the real seeded/backfilled data: history pagination, `subject_id` filtering, and the class-detail endpoint all matched the automated tests' expectations exactly against genuinely-populated data (28 total sessions, 18 present across 3 subjects); confirmed a class the student isn't enrolled in correctly 403s; confirmed the faculty side (`/faculty/me/sessions?status=ENDED` + the roster endpoint) returns the expected `status: "ENDED"` on real historical sessions.

Frontend:
- [x] `src/lib/types.ts` — `AttendanceHistoryItem`, `Pagination`, `AttendanceHistory`, `SubjectAttendanceSummary`, `AttendanceSummary`, `ClassAttendance`; `SessionRoster.session` gained `status`.
- [x] `src/queries/use-attendance-history.ts`, `use-attendance-summary.ts`, `use-class-attendance.ts`, `use-faculty-sessions.ts` — same conventions as every prior phase's query hooks.
- [x] `src/queries/use-session-roster.ts` — `refetchInterval` is now a function that stops polling once `session.status === "ENDED"` (docs/UI.md's own "avoid unnecessary complexity" spirit — no reason to poll a roster that can no longer change).
- [x] `src/app/student/history/page.tsx` — overall percentage, a per-subject breakdown list (docs/UI.md §7/§23 pattern, each row linking into the per-class detail page), and a paginated chronological attendance list (docs/UI.md §22's mockup, simplified per §63's "Priority 4, avoid unnecessarily complex" guidance — a flat list with Prev/Next rather than a calendar or month grouping).
- [x] `src/app/student/history/[classId]/page.tsx` — docs/UI.md §23 "Student Subject Detail": percentage + fraction + a chronological records list for just that class.
- [x] `src/app/faculty/sessions/page.tsx` — new static index route alongside the existing `sessions/new` and `sessions/[sessionId]` (no routing conflict, same precedent as Phase 4's static-before-dynamic ordering discovery, just automatic here since Next resolves a folder's own `page.tsx` before any dynamic sibling): a flat list of the faculty's own ended sessions (`GET /faculty/me/sessions?status=ENDED`) linking into the existing roster page for full present/total detail — no inline per-row counts, since that would need a new aggregating endpoint just for this list (docs/UI.md §63: history is lower-priority, don't overbuild it).
- [x] `src/app/faculty/sessions/[sessionId]/page.tsx` — the LIVE badge is now conditional on `session.status === "ACTIVE"` (shows "Ended" otherwise), matching the roster hook's own polling change.
- [x] Dashboard links added: student dashboard → "View Full Attendance History" (`/student/history`); faculty dashboard → "View Session History" (`/faculty/sessions`).
- [x] Build + lint clean (`npm run build` / `npm run lint`); all 12 routes (including the two new ones) compiled with no path conflicts.
- [x] **Verified by the user in their own browser.**
### Phase 7 — Security hardening completion — NOT STARTED
### Phase 8 — Reports & admin — NOT STARTED
### Phase 9 — Testing gaps + deployment prep — NOT STARTED

## Known doc inconsistencies (not yet resolved in `docs/`)
- `docs/DATABASE.md` §46 lists `audit_logs` as a "potential post-MVP" entity, but `docs/PRODUCT.md` §25 lists "audit information" under MVP scope. Plan: build it in Phase 7 regardless; update `docs/DATABASE.md` to reflect this once built.

Resolved: the verification-context schema gap (`docs/DATABASE.md` had no table matching `docs/API.md`'s state machine) was fixed in Phase 5a — see `docs/DATABASE.md` §27a and the Phase 5a log above.

## Environment notes
- Verified available in the dev environment: Node v22.23.1, npm 10.9.8, Python 3.10.12, `uv` 0.11.29, Docker 29.6.1 (daemon running), Docker Compose v5.3.1, git 2.34.1, psql 14.24.

## What's next
Phase 6b is done, verified in the browser. Stopped here for review per the working process in `PLAN.md`. Next up is Phase 7 — security hardening completion: widen Arcjet coverage (still entirely unapplied — `/auth/*` shipped without it in Phase 1 as an explicit, tracked gap), the rate-limit-bypass test, CORS lockdown for prod, security headers, a final cookie-flag pass, the `audit_logs` table + key events, and a consolidated regression run against the full `docs/SECURITY.md` §69 priority list.

## How to run this locally
**Preferred:** VS Code → Command Palette → "Tasks: Run Task" → **"GeoAttend: Start All"** (or run the three "GeoAttend: Docker/Backend/Frontend" tasks individually) — each opens its own dedicated terminal. Backed by `scripts/start-{docker,backend,frontend}.sh`; `start-backend.sh` waits for Postgres to be ready before starting uvicorn.

Manual equivalent:
- `docker compose up postgres` — starts Postgres (pgvector image) on port 5432.
- Backend: `cd backend && cp .env.example .env` (trim to just `ENVIRONMENT`/`DATABASE_URL`/`CORS_ALLOW_ORIGINS` for now), then `uv run uvicorn app.main:app --port 8001 --reload`.
- Frontend: `cd frontend && cp .env.example .env.local && npm run dev -- --port 3000`.
- Seed dev users (idempotent): `cd backend && uv run python scripts/seed.py` — creates `student@example.com` / `faculty@example.com` / `admin@example.com`, all with password `password123`.
- Backend tests: `cd backend && uv run pytest`. Lint: `uv run ruff check .` / `uv run ruff format .`.
- Frontend build/lint: `cd frontend && npm run build` / `npm run lint`.
