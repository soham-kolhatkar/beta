ENVIRONMENT=development

DATABASE_URL=postgresql+asyncpg://geoattend:geoattend@localhost:5432/geoattend

CORS_ALLOW_ORIGINS=["http://localhost:3000"]

# Phase 1+
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET=

# Phase 7
ARCJET_KEY=

# Phase 3/5b (tuned during those phases)
FACE_SIMILARITY_THRESHOLD=

# Phase 5a (tuned during that phase)
LOCATION_MIN_ACCURACY_METERS=
VERIFICATION_CONTEXT_TTL_SECONDS=
